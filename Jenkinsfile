pipeline {
    agent any
    stages {
        stage ("build") {
            step {
                sh "echo building application."
            }
        }
        stage ("test") {
            step {
                sh "echo testing application."
            }
        }
    }
}